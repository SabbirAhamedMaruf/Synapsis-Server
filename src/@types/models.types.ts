export type singleModelInfo = {
  name: string;
  description: string;
  provider: string;
  parameters: string[];
  tags: string[];
  pulls: string;
  updatedAt: string;
  isCloudModelAvailable: boolean;
};
